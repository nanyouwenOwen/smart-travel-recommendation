import { mount } from '@vue/test-utils';import { beforeEach,describe,expect,it,vi } from 'vitest';import LocationSearch from './LocationSearch.vue';import { realtimeApi } from '@/api/realtime'
vi.mock('@/api/realtime',()=>({realtimeApi:{searchLocations:vi.fn()}}))
describe('LocationSearch',()=>{beforeEach(()=>vi.clearAllMocks());it('only searches after explicit click and emits an opaque location id',async()=>{vi.mocked(realtimeApi.searchLocations).mockResolvedValue([{locationId:'loc-1',name:'京都',displayName:'京都市，日本',latitude:35,longitude:135,timezone:'Asia/Tokyo',sources:[]}]);const wrapper=mount(LocationSearch,{props:{modelValue:'京都','onUpdate:modelValue':()=>{}}});expect(realtimeApi.searchLocations).not.toHaveBeenCalled();await wrapper.get('button').trigger('click');await vi.waitFor(()=>expect(wrapper.text()).toContain('京都市'));await wrapper.get('.location-result').trigger('click');const events=wrapper.emitted('update:selectedId')??[];expect(events[events.length-1]).toEqual(['loc-1'])});it('rejects short queries without a provider call',async()=>{const wrapper=mount(LocationSearch,{props:{modelValue:'京'}});await wrapper.get('button').trigger('click');expect(realtimeApi.searchLocations).not.toHaveBeenCalled();expect(wrapper.text()).toContain('至少 2 个字符')})})
